# TODO: google_storage_bucket "this" using var.name / var.location
# - uniform_bucket_level_access = true
# - versioning { enabled = true }
# - lifecycle_rule { condition { age = var.age_days } action { type = "Delete" } }
